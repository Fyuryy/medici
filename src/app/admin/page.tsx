'use client'

import styles from '@/styles/AdminScreens.module.css'

export default function AdminHomePage() {
  return (
    <div className={styles.wrapper}>
      <div className={styles.stack}>
        <h1 className={styles.title}>Salut l’Admin !</h1>
      </div>
    </div>
  )
}
